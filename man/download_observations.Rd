% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/om-api-download.R
\name{download_observations}
\alias{download_observations}
\title{Download observations (e.g. as a shapefile)}
\arguments{
\item{constraints}{A \code{\link[=Constraints]{Constraints()}} object including \code{format} and
\code{includeData} as needed.}

\item{save_path}{Optional path for the downloaded archive. A temporary file
is used when omitted.}

\item{unzip}{If \code{TRUE}, unzip the archive and locate a \code{.shp} file.}

\item{verbose}{Print the request URL (token obfuscated).}
}
\value{
A list with \code{zip_path}, and when \code{unzip = TRUE} also
\code{extract_dir} and \code{shp_path}.
}
\description{
Requests the OM-API \code{observations} endpoint with constraints such as
\code{format = "SHAPEFILE"} and \code{includeData = FALSE}. The response is
saved as a file (typically a ZIP archive) and, by default, extracted to locate
the \code{.shp} file.
}
\details{
Call as \code{client$download_observations(constraints, save_path, unzip, verbose)}
on a \link{DABClient}, \link{WHOSClient}, or \link{HISCentralClient} object.
}
\examples{
\dontrun{
client <- HISCentralClient(token = "my-token")
areas <- client$get_properties(PREDEFINED_SEARCH_AREA, limit = 10)
dl <- client$download_observations(Constraints(
  predefinedSearchArea = areas$get_item(1)$value,
  beginPosition = "2026-04-01T00:00:00Z",
  endPosition = "2026-04-30T23:59:59Z",
  format = "SHAPEFILE",
  includeData = FALSE
))
}

}
\seealso{
\code{\link[=Constraints]{Constraints()}}
}
